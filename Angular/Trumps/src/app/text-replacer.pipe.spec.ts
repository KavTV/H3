import { TextReplacerPipe } from './text-replacer.pipe';

describe('TextReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new TextReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
